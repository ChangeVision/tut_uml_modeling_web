# frozen_string_literal: true

source 'https://rubygems.org'

ruby '3.4.4'

gem 'logger', '~> 1.6'   # Ruby 3.4 で bundled gem になったため明示
gem 'ostruct', '~> 0.6'  # Ruby 3.4 で stdlib から外れたため明示
gem 'rackup', '~> 2.1'
gem 'sinatra', '~> 4.0'
gem 'sqlite3', '~> 2.0'  # 1.7 系から 2.x 系へアップデート（Ruby 3.4 対応）
gem 'webrick', '~> 1.8'

group :development do
  gem 'rerun', '~> 0.14'
end
